1.该模板的异步发送和异步接收采用的是线程安全队列，发送消息部分会出现 **粘包** 情况，为了解决该问题，本模板提供了一种解决模式，即发送消息前会将字节包重新封装：**在获取发送字符串的字节前先加压入一个4字节的大端序，再压入字符串的字节，最后才发送过去**。	因此，服务器在接收前需要对接收的字节包进行拆包：**先解大端序（4字节），之后的才是包内容**



2.大端序为4个字节00 00 00 00，如00 00 00 0A表示的是发送的字节包内容长度为11，在服务器可以对大端序进行**十六进制处理**，获取转为十进制的整数，可以得到需要包内容字节长度。

